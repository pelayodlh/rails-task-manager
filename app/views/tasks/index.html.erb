<h1>My tasks!</h1>

<% @tasks.each do |task|%>
<div>
<%= link_to task.title, task_path(task.id) %> <%= link_to "Delete?", task_path(task.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
</div>
<% end %>
<hr>
<div>
<%= link_to "Want to add a new one?", tasks_new_path %>
</div>
